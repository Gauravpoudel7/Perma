/**
 * "SOL/USDC · Orca Whirlpool" (verbatim). The live spot price lives in the
 * TopBar, mounted once for every route — not repeated here.
 */
export function MarketHeader() {
  return <p className="text-body-sm mt-1 text-text-muted">SOL/USDC · Orca Whirlpool</p>;
}
